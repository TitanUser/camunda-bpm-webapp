  'use strict';

  module.exports = [
    '$scope',
    // 'Views',
    function(
    $scope
    // Views
  ) {

      $scope.processesListVars = { read: [ 'processesListData' ] };
      // $scope.processesListPlugins = Views.getProviders({ component: 'tasklist.processesList' });
    }];
